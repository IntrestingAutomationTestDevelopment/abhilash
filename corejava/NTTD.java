package corejava;

public class NTTD {
	/**
	 * SecurityTesting Interview Questions AND Answers
	 */
	public static void securityTesting() {
		/*
		 * vulnerabilities you test SQL Injection Cross-Site Scripting (XSS) Cross-Site
		 * Request Forgery (CSRF) Broken Authentication Insecure Direct Object
		 * References (IDOR) Security Misconfigurations Sensitive Data Exposure
		 * Server-Side Request Forgery (SSRF)
		 */
	}

	public static void owasp() {
		/**
		 * Broken Access Control Cryptographic Failures Injection Insecure Design
		 * Security Misconfiguration Vulnerable and Outdated Components Identification
		 * and Authentication Failures Software and Data Integrity Failures Security
		 * Logging and Monitoring Failures Server-Side Request Forgery (SSRF)
		 */
		/**
		 * Secure means safe, protected, or confident, while insecure means lacking
		 * confidence or security.
		 */
	}

	public static void penetrationTesting() {
		/**
		 * Static Application Security Testing (SAST): SonarQube, Checkmarx, Fortify
		 * Dynamic Application Security Testing (DAST): OWASP ZAP, Burp Suite
		 * Penetration Testing: Metasploit, Kali Linux Vulnerability Scanning: Nessus,
		 * Qualys API Security Testing: Postman, ReadyAPI, OWASP ZAP Network Security
		 * Testing: Nmap, Wireshark
		 */
	}
	/**
	 * Security No encryption; data is transmitted in plain text. 
	 * Secure communication using encryption (via SSL/TLS). 
	 * Encryption Not encrypted,  making data vulnerable to interception and attacks. Encrypted using SSL/TLS,
	 * ensuring data confidentiality. Certificate No SSL/TLS certificate is
	 * required. Requires an SSL/TLS certificate to establish trust. Port Operates
	 * on port 80. Operates on port 443. Speed Slightly faster due to no encryption
	 * overhead. Slightly slower due to encryption/decryption overhead. Data
	 * Integrity Data can be altered during transmission. Ensures data integrity;
	 * unauthorized modifications are detectable. Authentication No verification of
	 * the server’s identity. Verifies the identity of the server using
	 * certificates. SEO Impact Not favored by search engines. Preferred by search
	 * engines, boosting rankings. Use Cases Suitable for non-sensitive data (e.g.,
	 * blogs). Ideal for sensitive data (e.g., banking, e-commerce). Visual
	 * Indicator Browsers show "Not Secure" for HTTP sites. Displays a padlock icon
	 * and "Secure" in browsers. Why HTTPS is Better
	 */
}
